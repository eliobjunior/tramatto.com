{
  "name": "proxy",
  "version": "1.0.0",
  "description": "Worker Cloudflare que fica entre o frontend estático (`tramatto.com`) e a API oficial da Nuvemshop. É o único lugar do projeto autorizado a conhecer o `access_token` da loja. O frontend nunca recebe `access_token`, `client_secret` ou qualquer header `Authorization`.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
